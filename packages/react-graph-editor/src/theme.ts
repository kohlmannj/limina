export interface IScrollBarTheme {
  thumbColor?: string;
  thumbWidth?: number;
  trackColor?: string;
  trackWidth?: number;
}

export const defaultTheme = {
  thumbColor: '#666',
  thumbWidth: 7.5,
  trackColor: '#efefef',
  trackWidth: 15,
};
